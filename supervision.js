const data=window.SUPERVISION_DATA||[],list=document.getElementById('record-list'),search=document.getElementById('search'),year=document.getElementById('year-filter'),role=document.getElementById('role-filter'),count=document.getElementById('result-count'),active=document.getElementById('active-filter'),empty=document.getElementById('empty');[...new Set(data.map(x=>x.year))].sort().reverse().forEach(y=>{const o=document.createElement('option');o.value=y;o.textContent=y==='Unspecified'?'Year not recorded':y;year.append(o)});const esc=s=>String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));function render(){const q=search.value.trim().toLowerCase(),rows=data.filter(x=>(year.value==='all'||x.year===year.value)&&(role.value==='all'||x.role===role.value)&&(!q||`${x.name} ${x.title} ${x.program}`.toLowerCase().includes(q)));count.textContent=`${rows.length} record${rows.length===1?'':'s'}`;active.textContent=q||year.value!=='all'||role.value!=='all'?'Filtered results':'Showing all supervision';empty.hidden=rows.length>0;list.innerHTML=rows.map(x=>`<article class="record"><div class="year">${esc(x.year)}</div><div><h2>${esc(x.title)}</h2><div class="student">${esc(x.name)}</div><div class="meta">${esc(x.program)} · ${esc(x.role)} supervisor${x.date?` · ${esc(x.date)}`:''}</div></div><div class="status"><span>${esc(x.status)}</span></div></article>`).join('')}search.addEventListener('input',render);year.addEventListener('change',render);role.addEventListener('change',render);render();
